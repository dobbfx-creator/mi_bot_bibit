"estrategia": 
  "tf": "15m",                    // Timeframe de las velas que usa la estrategia

  "vol_relativo": true,           // Usa volumen relativo vs. media
  "vol_ma_n": 20,                 // Período de la media de volumen
  "vol_min_mult": 1.3,            // Mínimo: vol actual >= 1.3x vol medio
  "ancho_min_pct": 0.35,          // Ancho mínimo de bandas (evita compresión extrema)

  "bb_len": 20,                   // Período de Bandas de Bollinger
  "bb_mult": 2.0,                 // Desvío estándar para Bollinger (tamaño de banda)

  "usar_ema200": true,            // Filtro de dirección con EMA200
  "ema_len": 200,                 // Período de la EMA usada

  "usar_adx": true,               // Filtro de fuerza de tendencia (ADX)
  "adx_len": 14,                  // Período del ADX
  "adx_min": 20,                  // Mínimo ADX para habilitar entradas

  "usar_squeeze": true,           // Exige expansión respecto al promedio de ancho
  "bb_width_ma_len": 40,          // Período del promedio de “ancho de bandas”
  "squeeze_mult": 1.20,           // Umbral: ancho actual > promedio * 1.20

  "usar_rsi": false,              // Guardia con RSI (apagado por defecto)
  "rsi_len": 14,                  // Período del RSI
  "rsi_long_min": 55,             // Mínimo RSI para longs (si se usa)
  "rsi_short_max": 45             // Máximo RSI para shorts (si se usa)

   trailing":
  "activar_pct": 0.5,             // Activa trailing cuando el precio avanza >= 0.5% desde la entrada
  "buffer_pct": 0.18,             // Margen de seguridad al colocar el SL detrás del pivote (en %)
  "pivote_tf": "15m",             // Timeframe del pivote que usa para mover el SL
  "cooldown_seg": 45,             // Mínimos segundos entre intentos de mover el SL
  "min_mov_sl_pct": 0.05          // Cambio mínimo del SL (en %) para enviar actualización

"use_ema200_slope": true,                 // Exige pendiente mínima de la EMA200 (fuerza de tendencia, no solo estar arriba/abajo)
"ema200_slope_min_pct_per_bar": 0.07,     // Umbral de pendiente por vela (en % por vela). Ej: 0.07 = 0.07% por vela

"use_rsi_guard": true,                    // Filtro con RSI para no comprar “agotado” o sin impulso
"rsi_delta_min": 0.7,                     // RSI debe mejorar al menos este delta (impulso). Para shorts, caer al menos este delta
"rsi_overbought": 68,                     // Tope de sobrecompra para longs (si RSI > 68, no comprar)

"use_adx_rising": true,                   // Exige que el ADX esté subiendo (no solo alto)
"adx_delta_min": 0.7,                     // Mínima suba del ADX respecto a la vela previa

"use_min_dist_ema200": true,              // Pide distancia mínima al EMA200 (evita entrar “pegado” a la media)
"min_dist_ema200_pct": 0.5,               // Distancia mínima en % (ej: 0.5% o más lejos de la EMA200)

"use_breakout_retest": false,             // Si hubo ruptura grande, esperar “retest/confirmación” antes de entrar
"breakout_retest_min_atr_mult": 1.0,      // ¿Qué tan grande es “ruptura grande”? múltiplos de ATR (ej: 1.0 x ATR)
"confirm_wait_bars": 2,                   // Velas de espera/confirmación sin invalidar (post ruptura)

"use_atr": false,                          // Guardia opcional basada en ATR (placeholder: encendido solo si querés reglas extra con ATR)
"atr_period": 14                           // Período del ATR (si lo usás para guards o retest)
